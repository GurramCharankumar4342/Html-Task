# Html-Task
HTML (HyperText Markup Language) is the standard language for creating webpages. It includes tags for text, links, images, forms, tables, lists, media, and layout. HTML5 adds semantic elements like &lt;section>, &lt;header>, and &lt;footer> to improve structure and accessibility.
